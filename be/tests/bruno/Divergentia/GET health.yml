info:
  name: GET health
  type: http
  seq: 2

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{port}}/api/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
