info:
  name: GET supported formats
  type: http
  seq: 3

http:
  method: GET
  url: "{{protocol}}://{{hostname}}:{{port}}/api/formats/supported"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
